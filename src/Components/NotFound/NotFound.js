import React from 'react';

const NotFound = () => {
    return (
        <div className="mt-5">
            <h1 className="text-center">Sorry, the page you are looking for doesn't exists</h1>
        </div>
    );
};

export default NotFound;